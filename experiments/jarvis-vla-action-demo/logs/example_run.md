# Example Run

This log was generated by `vla_action_demo.py`.
It is a toy JARVIS-VLA-style action-interface demo, not official model inference.

## tree_collect_wood

- Observation: A tree trunk is centered in front of the player.
- Instruction: Collect wood.

### Relevant State

```json
{
  "distance_to_target": "near",
  "inventory": [
    "empty_hand"
  ],
  "time": "day",
  "visible_objects": [
    "tree_trunk"
  ]
}
```

### Candidate Actions

`move_forward`, `turn_left`, `turn_right`, `wait`, `attack`

### Selected Action

```json
{
  "action": "attack",
  "arguments": {
    "target": "tree_trunk"
  },
  "confidence": 0.9,
  "explanation": "The tree trunk is centered and collecting wood maps to an attack/mine action."
}
```

### Explanation

The tree trunk is centered and collecting wood maps to an attack/mine action.

### Why Pure Language Planning Is Insufficient

A high-level plan says collect wood, but the controller still needs a grounded attack action on the trunk.

### How VLA-Style Action Prediction Differs

VLA prediction binds the instruction to the visible trunk and emits an executable attack action.

## craft_table_planks

- Observation: A crafting table is visible and the player has logs.
- Instruction: Craft planks or sticks.

### Relevant State

```json
{
  "distance_to_target": "near",
  "inventory": [
    "oak_log"
  ],
  "time": "day",
  "visible_objects": [
    "crafting_table"
  ]
}
```

### Candidate Actions

`move_forward`, `turn_left`, `turn_right`, `wait`, `use`, `craft_item`

### Selected Action

```json
{
  "action": "use",
  "arguments": {
    "target": "crafting_table"
  },
  "confidence": 0.86,
  "explanation": "The crafting table is the correct interface before selecting planks or sticks."
}
```

### Explanation

The crafting table is the correct interface before selecting planks or sticks.

### Why Pure Language Planning Is Insufficient

A language plan may say craft sticks, but action prediction must choose the crafting table interaction first.

### How VLA-Style Action Prediction Differs

VLA prediction chooses an interaction with the visible station before recipe selection.

## furnace_smelt_iron

- Observation: A furnace is visible in front of the player.
- Instruction: Smelt iron ore.

### Relevant State

```json
{
  "distance_to_target": "near",
  "inventory": [
    "iron_ore",
    "coal"
  ],
  "time": "day",
  "visible_objects": [
    "furnace"
  ]
}
```

### Candidate Actions

`move_forward`, `turn_left`, `turn_right`, `wait`, `use`

### Selected Action

```json
{
  "action": "use",
  "arguments": {
    "target": "furnace"
  },
  "confidence": 0.91,
  "explanation": "The furnace is visible and the inventory contains iron ore and fuel."
}
```

### Explanation

The furnace is visible and the inventory contains iron ore and fuel.

### Why Pure Language Planning Is Insufficient

A plan says smelt iron, but the next low-level action is use furnace, not mine or craft.

### How VLA-Style Action Prediction Differs

VLA prediction maps visual furnace evidence and inventory state to the use action.

## iron_wooden_pickaxe

- Observation: Iron ore is visible in a cave wall.
- Instruction: Obtain iron ore.

### Relevant State

```json
{
  "distance_to_target": "near",
  "inventory": [
    "wooden_pickaxe"
  ],
  "time": "underground",
  "visible_objects": [
    "iron_ore"
  ]
}
```

### Candidate Actions

`move_forward`, `turn_left`, `turn_right`, `wait`, `attack`, `equip_item`

### Selected Action

```json
{
  "action": "equip_item",
  "arguments": {
    "item": "stone_pickaxe_or_better"
  },
  "confidence": 0.82,
  "explanation": "Mining iron with a wooden pickaxe would fail, so the next action is to switch or obtain a valid tool."
}
```

### Explanation

Mining iron with a wooden pickaxe would fail, so the next action is to switch or obtain a valid tool.

### Why Pure Language Planning Is Insufficient

A plan might say mine iron, but action prediction must reject an invalid tool/action pair.

### How VLA-Style Action Prediction Differs

VLA prediction can encode action preconditions and avoid executing a doomed mining action.

## night_mob_survive

- Observation: A hostile mob is nearby at night.
- Instruction: Survive.

### Relevant State

```json
{
  "distance_to_target": "near",
  "inventory": [
    "dirt",
    "wooden_sword"
  ],
  "time": "night",
  "visible_objects": [
    "zombie",
    "open_ground"
  ]
}
```

### Candidate Actions

`move_forward`, `turn_left`, `turn_right`, `wait`, `attack`, `retreat`, `place_block`

### Selected Action

```json
{
  "action": "retreat",
  "arguments": {
    "threat": "zombie"
  },
  "confidence": 0.88,
  "explanation": "Immediate survival requires increasing distance before building or fighting."
}
```

### Explanation

Immediate survival requires increasing distance before building or fighting.

### Why Pure Language Planning Is Insufficient

A plan says survive, but immediate control should prioritize retreat or defense based on the visible threat.

### How VLA-Style Action Prediction Differs

VLA prediction reacts to visual threat state rather than only following a long-horizon plan.
